# Dependencies
node_modules/

# Environment / secrets — never commit real values, but keep the template
.env
.env.*
!.env.example

# Local data & artifacts (account DB, tokens, backups — SENSITIVE, never commit)
data/
backups/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Build output
dist/
release/

# Logs & temp files
logs/
*.log
*.tmp
*.temp

# Test coverage
coverage/
.vitest/

# Editor / OS
.vscode/
.idea/
.vs/
.DS_Store
Thumbs.db
